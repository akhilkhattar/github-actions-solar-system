apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "false"
  name: solar-system
  namespace: _{_NAMESPACE_}_
spec:
    rules:
    - host: a270c7ae4dc8d4343a11395e5a4fd3e1-1067918871.us-east-1.elb.amazonaws.com
      http:
        paths:
        - backend:
            service:
              name: solar-system
              port:
                number: 3000
          path: /
          pathType: Prefix
    # tls:
    # - hosts:
    #   - solar-system.test.kolly38.com
    #   secretName: ingress-local-tls